# Private nb-connect credentials.
# Copy this file to ~/.config/nb-connect/credentials.yaml, replace the value,
# and keep the resulting file out of source control.
netbox:
  token: replace-with-your-netbox-api-token